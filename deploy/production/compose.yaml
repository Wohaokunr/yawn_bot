name: yawnbot

services:
  yawnbot:
    image: "${YAWNBOT_IMAGE:?YAWNBOT_IMAGE must be an immutable digest reference}"
    restart: unless-stopped
    env_file:
      - .env
      - onebot.env
    environment:
      HOST: 0.0.0.0
      PORT: 8080
      LOCALSTORE_USE_CWD: "true"
      YAWNBOT_AUTO_MIGRATE: "false"
      FANQIE_BROWSER_WS_ENDPOINT: "${FANQIE_BROWSER_WS_ENDPOINT:-}"
    ports:
      - "127.0.0.1:${YAWNBOT_PORT:-8080}:8080"
    volumes:
      - ./data:/app/data
    networks:
      - yawnbot-internal
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/healthz', timeout=3).read()
      interval: 30s
      timeout: 5s
      start_period: 30s
      retries: 3

  playwright:
    image: "${YAWNBOT_BROWSER_IMAGE:-busybox:1.36}"
    profiles:
      - fanqie-browser
    restart: unless-stopped
    init: true
    shm_size: "512m"
    networks:
      - yawnbot-internal

networks:
  yawnbot-internal:
    external: true
    name: yawnbot-internal
