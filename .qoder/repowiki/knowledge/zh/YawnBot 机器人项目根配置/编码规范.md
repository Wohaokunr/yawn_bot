- 插件统一放置在 `src/plugins` 目录下，并通过 `nb plugin create` 脚手架生成，遵循 NoneBot2 插件发现机制。
- 环境变量按环境拆分到 `.env.dev` 与 `.env.prod`，由 NoneBot 自动加载，禁止硬编码敏感配置。
- 代码风格与类型检查统一通过 Ruff 和 Pyright 配置在 `pyproject.toml` 中集中管理，并针对 NoneBot2 API 显式忽略不兼容规则。
- 依赖版本通过 `uv.lock` 精确锁定，禁止手动修改 `pyproject.toml` 中的版本号而不重新运行 `uv lock`。