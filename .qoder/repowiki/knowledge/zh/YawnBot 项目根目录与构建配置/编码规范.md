- 插件统一放在 `src/plugins` 目录下，由 nonebot2 自动发现加载。
- 所有第三方 nonebot 插件在 `pyproject.toml` 的 `[tool.nonebot.plugins]` 中显式声明模块名映射。
- 代码风格与静态检查通过 Ruff 统一配置，忽略 nonebot2 特有的导入与注解规则（如 E402、B008、UP037）。
- 环境变量按环境拆分到 `.env.dev` 与 `.env.prod`，通过 LOG_LEVEL 等键控制运行行为。